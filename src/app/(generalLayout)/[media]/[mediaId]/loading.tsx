import Loader from '@/components/ui/Loader'
import React from 'react'

const loading = () => {
    return (
        <main className='flex items-center justify-center'><Loader /></main>
    )
}

export default loading
